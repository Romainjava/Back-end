package javatest;

class multiplication {
	public static void main(String[] arg){ //*ligne de code
		
		int mult = 3; //* type de variable int= nombre entier string= phrase// et table de 3
	    for (int i = 1; i <11; i++) //* type de variable for=pour qui crée un boucle
	    { 
	System.out.println ( i + " x " +  mult  + " = " + (i*mult)); //* i= nom donnée " x " =afficher le signe x (entre " " rpz un texte à afficher dans la console)
	        //* mult variable qu'on a nommé et appeler et le "=" pour l'afficher
	}
	
	}
}
